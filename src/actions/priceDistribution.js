export const handlePriceDistributionResults = (priceDistributionResults) => ({
	type: "ADD_PRICE_RESULT",
	priceDistributionResults
});

export const handleResults = (countyResults) => ({
	type: "ADD_RESULT",
	countyResults
});
